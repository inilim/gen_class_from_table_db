<?php

namespace {{ namespace }};

{% if extends.exists %}
use {{ extends.use }};
{% endif %}

/**
* "{{ table.name }}"
*/
{{ final ? 'final ' : '' }}{{ readonly ? 'readonly ' : '' }}class {{ class_name }}{{ extends.exists ? ' extends ' ~ extends.class_name : '' }}
{
{% for item in const %}
    {{ item.visibility }} const {{ item.name }} = {{ item.value_export|raw }};
{% endfor %}

    function __construct(
    {% for item in cols %}
    {{ item.visibility }} {{ item.is_null ? '?' : '' }}{{ item.type }} ${{ item.name }}{% if loop.last == false %},{% endif %} 
    {% endfor %}
    ) {
    }
}