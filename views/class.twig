<?php

namespace {{ namespace }};

{% if extends.exists %}
use {{ extends.use }};
{% endif %}

/**
 * "{{ table.getName() }}"
 */
{{ final ? 'final ' : '' }}{{ readonly ? 'readonly ' : '' }}class {{ class_name }}{{ extends.exists ? ' extends ' ~ extends.class_name : '' }}
{
{% for item in const %}
    {{ item.getVisibility() }} const {{ item.getName() }} = {{ item.getValueExport(|raw }};
{% endfor %}

    function __construct(
    {% for item in cols %}
    {{ item.getVisibility() }} {{ item.isNull() ? '?' : '' }}{{ item.getType() }} ${{ item.getName() }}{% if loop.last == false %},{% endif %} 
    {% endfor %}
    ) {
    }
}