<?php

namespace {{ namespace }};

{% if extends.exists %}
use {{ extends.use }};
{% endif %}

/**
 * "{{ table.getName() }}"
{% for item in cols %}
 * @property {{ item.isNull() ? '?' : '' }}{{ item.getType() }} ${{ item.getName() }}
{% endfor %}
 */
#[\AllowDynamicProperties]
{{ final ? 'final ' : '' }}class {{ class_name }}{{ extends.exists ? ' extends ' ~ extends.class_name : '' }}
{
{% for item in const %}
    {{ item.getVisibility() }} const {{ item.getName() }} = {{ item.getValueExport()|raw }};
{% endfor %}
}