<?php

namespace {{ namespace }};

{% if extends.exists %}
use {{ extends.use }};
{% endif %}

/**
 * "{{ table.name }}"
{% for item in cols %}
 * @property {{ item.is_null ? '?' : '' }}{{ item.type }} ${{ item.name }}
{% endfor %}
 */
#[\AllowDynamicProperties]
{{ final ? 'final ' : '' }}class {{ class_name }}{{ extends.exists ? ' extends ' ~ extends.class_name : '' }}
{
{% for item in const %}
    {{ item.visibility }} const {{ item.name }} = {{ item.value_export|raw }};
{% endfor %}
}